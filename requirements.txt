# Web server
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
jinja2>=3.1.0
aiofiles>=23.2.0
pydantic>=2.5.0,<3.0

# OCR — BAH pipeline (Tesseract-based, headless OpenCV)
opencv-python-headless>=4.8.0
pytesseract>=0.3.10

# OCR — Classifier pipeline (PaddleOCR PP-StructureV3 + table recognition)
# Pin paddleocr>=2.8.0 for PP-StructureV3 / TableRecognitionPipelineV2 support.
# Use paddlepaddle for CPU; swap for paddlepaddle-gpu on GPU hosts.
paddleocr>=2.8.0
paddlepaddle>=2.6.0

# ML classifier
scikit-learn>=1.4.0
joblib>=1.3.0
pyspellchecker>=0.8.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Reporting & document generation
reportlab>=4.0.0
Pillow>=10.0.0

# Utilities
numpy>=1.26.0,<2.0
requests>=2.31.0
